<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_company_form" model="ir.ui.view">
        <field name="name">ece online view_company_form</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Documentos electrónicos" name="ece">
                    <group>
                        <field name="firma_id"/>
                        <field name="ambiente_id"/>
                        <field name="date"/>
                        <field name="takes_accounting"/>
                        <field name="agent_retention"/>
                        <field name="ar_number_resolution" attrs="{'invisible': [('agent_retention', '=', False)], 'required': [('agent_retention', '=', True)]}"/>
                        <field name="regime_micro"/>
                        <field name="regime_rimpe"/>
                        <field name="is_special_taxpayer_number"/>
                        <field name="special_taxpayer_number" attrs="{'invisible': [('is_special_taxpayer_number', '=', False)], 'required': [('is_special_taxpayer_number', '=', True)]}"/>

                    </group>
                </page>
            </xpath>
        </field>
    </record>

</odoo>
